export const DAILY_FREE_PREVIEW_POLICY = Object.freeze({
  entitlementId: 'daily_free_preview', allowancePerConsumerAccount: 1,
  resetCadence: 'calendar_day', authority: 'server', consumeWhen: 'accepted_rtc_connection', enabled: false,
});
