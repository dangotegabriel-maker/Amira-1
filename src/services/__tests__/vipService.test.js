import { canSeeProfileVisitors, getVipTier, hasVipLevel } from '../vipService';
describe('VIP entitlements',()=>{test('FREE cannot reveal visitor identities',()=>expect(canSeeProfileVisitors({vip:{tier:'FREE',status:'inactive'}})).toBe(false));test('VIP_1 can reveal identities',()=>expect(canSeeProfileVisitors({vip:{tier:'VIP_1',status:'active'}})).toBe(true));test('hierarchy grants lower-level capability only while active',()=>{expect(hasVipLevel({vip:{tier:'VIP_3',status:'active'}},'VIP_2')).toBe(true);expect(getVipTier({vip:{tier:'VIP_3',status:'expired'}})).toBe('FREE');});});
