import React,{useCallback,useRef,useState} from 'react';
import {useFocusEffect} from '@react-navigation/native';
import {ActivityIndicator,FlatList,Image,StyleSheet,Text,TouchableOpacity,View} from 'react-native';
import {Eye,Lock} from 'lucide-react-native';import {useUser} from '../../context/UserContext';import {isConsumer} from '../../models/userModel';import {profileViewService} from '../../services/profileViewService';import {COLORS} from '../../theme/COLORS';
const WhoViewedMeScreen=()=>{const {user}=useUser(),consumer=isConsumer(user),version=useRef(0);const [result,setResult]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(false);const load=useCallback(async()=>{if(!consumer||!user?.uid)return;const request=++version.current;setLoading(true);setError(false);try{const value=await profileViewService.result(user.uid);if(request===version.current)setResult(value);}catch(e){if(request===version.current){setResult(null);setError(true);}}finally{if(request===version.current)setLoading(false);}},[consumer,user?.uid]);useFocusEffect(useCallback(()=>{load();return()=>{version.current++;};},[load]));if(!consumer)return null;if(loading)return <View style={styles.center}><ActivityIndicator color={COLORS.primary}/></View>;if(error)return <View style={styles.center}><Text>Profile views could not be loaded.</Text><TouchableOpacity onPress={load}><Text style={styles.retry}>Try Again</Text></TouchableOpacity></View>;if(!result?.count)return <View style={styles.center}><Eye color={COLORS.primary} size={44}/><Text style={styles.title}>No profile views yet.</Text></View>;if(!result.reveal)return <View style={styles.center}><Lock color={COLORS.primary} size={44}/><Text style={styles.title}>{result.count} recent profile viewer{result.count===1?'':'s'}</Text><Text style={styles.body}>VIP is required to reveal Host identities.</Text></View>;return <FlatList style={styles.list} contentContainerStyle={{padding:18}} data={result.views} keyExtractor={item=>item.viewerUid} ListHeaderComponent={<Text style={styles.title}>Who Viewed Me</Text>} renderItem={({item})=><View style={styles.row}>{item.identity?.profilePic?<Image source={{uri:item.identity.profilePic}} style={styles.avatar}/>:<View style={styles.avatar}/>}<Text style={styles.name}>{item.identity?.username||'Host'}</Text></View>}/>;};
const styles=StyleSheet.create({center:{flex:1,backgroundColor:'#F7F7F9',alignItems:'center',justifyContent:'center',padding:28},list:{flex:1,backgroundColor:'#F7F7F9'},title:{fontSize:22,fontWeight:'900',color:COLORS.text,textAlign:'center',marginTop:16},body:{color:COLORS.textSecondary,textAlign:'center',marginTop:12},retry:{color:COLORS.primary,fontWeight:'800',marginTop:18},row:{flexDirection:'row',alignItems:'center',backgroundColor:'white',padding:14,borderRadius:16,marginTop:10},avatar:{width:44,height:44,borderRadius:22,backgroundColor:'#DDD'},name:{marginLeft:12,fontWeight:'800',color:COLORS.text}});export default WhoViewedMeScreen;
