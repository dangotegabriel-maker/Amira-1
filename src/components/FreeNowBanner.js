// Legacy socket events have no authorized identity or availability proof.
// Keep this dormant surface gated until an authoritative feature exists.
const FreeNowBanner = () => null;
export default FreeNowBanner;
